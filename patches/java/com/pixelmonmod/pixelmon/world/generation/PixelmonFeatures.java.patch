--- a/com/pixelmonmod/pixelmon/world/generation/PixelmonFeatures.java
+++ b/com/pixelmonmod/pixelmon/world/generation/PixelmonFeatures.java
@@ -153,16 +153,13 @@
 
 public class PixelmonFeatures {
     public static final ConfiguredFeature<TreeConfiguration, ?> PINAP_TREE = register(
-        "pinap_berry_tree",
-        Feature.TREE.configured(convert(BerryType.PINAP, new PinapFoliagePlacer(), new StraightTrunkPlacer(2, 0, 0)).ignoreVines().build())
+      "pinap_berry_tree", Feature.TREE.configured(convert(BerryType.PINAP, new PinapFoliagePlacer(), new StraightTrunkPlacer(2, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> BELUE_TREE = register(
-        "belue_berry_tree",
-        Feature.TREE.configured(convert(BerryType.BELUE, new BelueFoliagePlacer(), new StraightTrunkPlacer(4, 0, 0)).ignoreVines().build())
+      "belue_berry_tree", Feature.TREE.configured(convert(BerryType.BELUE, new BelueFoliagePlacer(), new StraightTrunkPlacer(4, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> CORNN_TREE = register(
-        "cornn_berry_tree",
-        Feature.TREE.configured(convert(BerryType.CORNN, new CornnFoliagePlacer(), new StraightTrunkPlacer(2, 0, 0)).ignoreVines().build())
+      "cornn_berry_tree", Feature.TREE.configured(convert(BerryType.CORNN, new CornnFoliagePlacer(), new StraightTrunkPlacer(2, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> MAGOST_TREE = register(
         "magost_berry_tree",
@@ -193,8 +190,7 @@
         Feature.TREE.configured(convert(BerryType.CHARTI, new ChartiFoliagePlacer(), new StraightTrunkPlacer(3, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> CHERI_TREE = register(
-        "cheri_berry_tree",
-        Feature.TREE.configured(convert(BerryType.CHERI, new CheriFoliagePlacer(), new StraightTrunkPlacer(4, 0, 0)).ignoreVines().build())
+      "cheri_berry_tree", Feature.TREE.configured(convert(BerryType.CHERI, new CheriFoliagePlacer(), new StraightTrunkPlacer(4, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> CHOPLE_TREE = register(
         "chople_berry_tree",
@@ -211,24 +207,20 @@
         "figy_berry_tree", Feature.TREE.configured(convert(BerryType.FIGY, new FigyFoliagePlacer(), new StraightTrunkPlacer(3, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> HABAN_TREE = register(
-        "haban_berry_tree",
-        Feature.TREE.configured(convert(BerryType.HABAN, new HabanFoliagePlacer(), new StraightTrunkPlacer(3, 0, 0)).ignoreVines().build())
+      "haban_berry_tree", Feature.TREE.configured(convert(BerryType.HABAN, new HabanFoliagePlacer(), new StraightTrunkPlacer(3, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> IAPAPA_TREE = register(
         "iapapa_berry_tree",
         Feature.TREE.configured(convert(BerryType.IAPAPA, new IapapaFoliagePlacer(), new StraightTrunkPlacer(2, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> AGUAV_TREE = register(
-        "aguav_berry_tree",
-        Feature.TREE.configured(convert(BerryType.AGUAV, new AguavFoliagePlacer(), new StraightTrunkPlacer(2, 0, 0)).ignoreVines().build())
+      "aguav_berry_tree", Feature.TREE.configured(convert(BerryType.AGUAV, new AguavFoliagePlacer(), new StraightTrunkPlacer(2, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> KASIB_TREE = register(
-        "kasib_berry_tree",
-        Feature.TREE.configured(convert(BerryType.KASIB, new KasibFoliagePlacer(), new StraightTrunkPlacer(4, 0, 0)).ignoreVines().build())
+      "kasib_berry_tree", Feature.TREE.configured(convert(BerryType.KASIB, new KasibFoliagePlacer(), new StraightTrunkPlacer(4, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> KEBIA_TREE = register(
-        "kebia_berry_tree",
-        Feature.TREE.configured(convert(BerryType.KEBIA, new KebiaFoliagePlacer(), new StraightTrunkPlacer(3, 0, 0)).ignoreVines().build())
+      "kebia_berry_tree", Feature.TREE.configured(convert(BerryType.KEBIA, new KebiaFoliagePlacer(), new StraightTrunkPlacer(3, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> KELPSY_TREE = register(
         "kelpsy_berry_tree",
@@ -243,8 +235,7 @@
         Feature.TREE.configured(convert(BerryType.HONDEW, new HondewFoliagePlacer(), new StraightTrunkPlacer(4, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> GREPA_TREE = register(
-        "grepa_berry_tree",
-        Feature.TREE.configured(convert(BerryType.GREPA, new GrepaFoliagePlacer(), new StraightTrunkPlacer(3, 0, 0)).ignoreVines().build())
+      "grepa_berry_tree", Feature.TREE.configured(convert(BerryType.GREPA, new GrepaFoliagePlacer(), new StraightTrunkPlacer(3, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> GANLON_TREE = register(
         "ganlon_berry_tree",
@@ -282,15 +273,13 @@
         Feature.TREE.configured(convert(BerryType.PAMTRE, new PamtreFoliagePlacer(), new StraightTrunkPlacer(4, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> NOMEL_TREE = register(
-        "nomel_berry_tree",
-        Feature.TREE.configured(convert(BerryType.NOMEL, new NomelFoliagePlacer(), new StraightTrunkPlacer(3, 0, 0)).ignoreVines().build())
+      "nomel_berry_tree", Feature.TREE.configured(convert(BerryType.NOMEL, new NomelFoliagePlacer(), new StraightTrunkPlacer(3, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> KEE_TREE = register(
         "kee_berry_tree", Feature.TREE.configured(convert(BerryType.KEE, new KeeFoliagePlacer(), new StraightTrunkPlacer(2, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> DURIN_TREE = register(
-        "durin_berry_tree",
-        Feature.TREE.configured(convert(BerryType.DURIN, new DurinFoliagePlacer(), new StraightTrunkPlacer(4, 0, 0)).ignoreVines().build())
+      "durin_berry_tree", Feature.TREE.configured(convert(BerryType.DURIN, new DurinFoliagePlacer(), new StraightTrunkPlacer(4, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> PAYAPA_TREE = register(
         "payapa_berry_tree",
@@ -307,23 +296,20 @@
         "mago_berry_tree", Feature.TREE.configured(convert(BerryType.MAGO, new MagoFoliagePlacer(), new StraightTrunkPlacer(4, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> NANAB_TREE = register(
-        "nanab_berry_tree",
-        Feature.TREE.configured(convert(BerryType.NANAB, new NanabFoliagePlacer(), new StraightTrunkPlacer(4, 0, 0)).ignoreVines().build())
+      "nanab_berry_tree", Feature.TREE.configured(convert(BerryType.NANAB, new NanabFoliagePlacer(), new StraightTrunkPlacer(4, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> ORAN_TREE = register(
         "oran_berry_tree", Feature.TREE.configured(convert(BerryType.ORAN, new OranFoliagePlacer(), new StraightTrunkPlacer(4, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> PECHA_TREE = register(
-        "pecha_berry_tree",
-        Feature.TREE.configured(convert(BerryType.PECHA, new PechaFoliagePlacer(), new StraightTrunkPlacer(4, 0, 0)).ignoreVines().build())
+      "pecha_berry_tree", Feature.TREE.configured(convert(BerryType.PECHA, new PechaFoliagePlacer(), new StraightTrunkPlacer(4, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> PETAYA_TREE = register(
         "petaya_berry_tree",
         Feature.TREE.configured(convert(BerryType.PETAYA, new PechaFoliagePlacer(), new StraightTrunkPlacer(3, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> POMEG_TREE = register(
-        "pomeg_berry_tree",
-        Feature.TREE.configured(convert(BerryType.POMEG, new PomegFoliagePlacer(), new StraightTrunkPlacer(3, 0, 0)).ignoreVines().build())
+      "pomeg_berry_tree", Feature.TREE.configured(convert(BerryType.POMEG, new PomegFoliagePlacer(), new StraightTrunkPlacer(3, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> RAZZ_TREE = register(
         "razz_berry_tree", Feature.TREE.configured(convert(BerryType.RAZZ, new RazzFoliagePlacer(), new StraightTrunkPlacer(3, 0, 0)).ignoreVines().build())
@@ -333,8 +319,7 @@
         Feature.TREE.configured(convert(BerryType.ROSELI, new RoseliFoliagePlacer(), new StraightTrunkPlacer(1, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> SALAC_TREE = register(
-        "salac_berry_tree",
-        Feature.TREE.configured(convert(BerryType.SALAC, new SalacFoliagePlacer(), new StraightTrunkPlacer(3, 0, 0)).ignoreVines().build())
+      "salac_berry_tree", Feature.TREE.configured(convert(BerryType.SALAC, new SalacFoliagePlacer(), new StraightTrunkPlacer(3, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> SITRUS_TREE = register(
         "sitrus_berry_tree",
@@ -345,8 +330,7 @@
         Feature.TREE.configured(convert(BerryType.TAMATO, new TamatoFoliagePlacer(), new StraightTrunkPlacer(4, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> WACAN_TREE = register(
-        "wacan_berry_tree",
-        Feature.TREE.configured(convert(BerryType.WACAN, new WacanFoliagePlacer(), new StraightTrunkPlacer(3, 0, 0)).ignoreVines().build())
+      "wacan_berry_tree", Feature.TREE.configured(convert(BerryType.WACAN, new WacanFoliagePlacer(), new StraightTrunkPlacer(3, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> PUMKIN_TREE = register(
         "pumkin_berry_tree",
@@ -363,40 +347,32 @@
         "strib_berry_tree", Feature.TREE.configured(convert(BerryType.STRIB, new StribFoliagePlacer(), new StribTrunkPlacer()).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> DRASH_TREE = register(
-        "drash_berry_tree",
-        Feature.TREE.configured(convert(BerryType.DRASH, new DrashFoliagePlacer(), new StraightTrunkPlacer(1, 0, 0)).ignoreVines().build())
+      "drash_berry_tree", Feature.TREE.configured(convert(BerryType.DRASH, new DrashFoliagePlacer(), new StraightTrunkPlacer(1, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> YACHE_TREE = register(
-        "yache_berry_tree",
-        Feature.TREE.configured(convert(BerryType.YACHE, new YacheFoliagePlacer(), new StraightTrunkPlacer(2, 0, 0)).ignoreVines().build())
+      "yache_berry_tree", Feature.TREE.configured(convert(BerryType.YACHE, new YacheFoliagePlacer(), new StraightTrunkPlacer(2, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> WEPEAR_TREE = register(
         "wepear_berry_tree",
         Feature.TREE.configured(convert(BerryType.WEPEAR, new WepearFoliagePlacer(), new StraightTrunkPlacer(3, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> TANGA_TREE = register(
-        "tanga_berry_tree",
-        Feature.TREE.configured(convert(BerryType.TANGA, new TangaFoliagePlacer(), new StraightTrunkPlacer(2, 0, 0)).ignoreVines().build())
+      "tanga_berry_tree", Feature.TREE.configured(convert(BerryType.TANGA, new TangaFoliagePlacer(), new StraightTrunkPlacer(2, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> STARF_TREE = register(
-        "starf_berry_tree",
-        Feature.TREE.configured(convert(BerryType.STARF, new StarfFoliagePlacer(), new StraightTrunkPlacer(2, 0, 0)).ignoreVines().build())
+      "starf_berry_tree", Feature.TREE.configured(convert(BerryType.STARF, new StarfFoliagePlacer(), new StraightTrunkPlacer(2, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> SHUCA_TREE = register(
-        "shuca_berry_tree",
-        Feature.TREE.configured(convert(BerryType.SHUCA, new ShucaFoliagePlacer(), new StraightTrunkPlacer(3, 0, 0)).ignoreVines().build())
+      "shuca_berry_tree", Feature.TREE.configured(convert(BerryType.SHUCA, new ShucaFoliagePlacer(), new StraightTrunkPlacer(3, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> ROWAP_TREE = register(
-        "rowap_berry_tree",
-        Feature.TREE.configured(convert(BerryType.ROWAP, new RowapFoliagePlacer(), new StraightTrunkPlacer(2, 0, 0)).ignoreVines().build())
+      "rowap_berry_tree", Feature.TREE.configured(convert(BerryType.ROWAP, new RowapFoliagePlacer(), new StraightTrunkPlacer(2, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> RINDO_TREE = register(
-        "rindo_berry_tree",
-        Feature.TREE.configured(convert(BerryType.RINDO, new RindoFoliagePlacer(), new StraightTrunkPlacer(4, 0, 0)).ignoreVines().build())
+      "rindo_berry_tree", Feature.TREE.configured(convert(BerryType.RINDO, new RindoFoliagePlacer(), new StraightTrunkPlacer(4, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> RAWST_TREE = register(
-        "rawst_berry_tree",
-        Feature.TREE.configured(convert(BerryType.RAWST, new RawstFoliagePlacer(), new StraightTrunkPlacer(1, 0, 0)).ignoreVines().build())
+      "rawst_berry_tree", Feature.TREE.configured(convert(BerryType.RAWST, new RawstFoliagePlacer(), new StraightTrunkPlacer(1, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> QUALOT_TREE = register(
         "qualot_berry_tree",
@@ -414,16 +390,14 @@
         "occa_berry_tree", Feature.TREE.configured(convert(BerryType.OCCA, new OccaFoliagePlacer(), new StraightTrunkPlacer(3, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> MICLE_TREE = register(
-        "micle_berry_tree",
-        Feature.TREE.configured(convert(BerryType.MICLE, new MicleFoliagePlacer(), new StraightTrunkPlacer(3, 0, 0)).ignoreVines().build())
+      "micle_berry_tree", Feature.TREE.configured(convert(BerryType.MICLE, new MicleFoliagePlacer(), new StraightTrunkPlacer(3, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> LIECHI_TREE = register(
         "liechi_berry_tree",
         Feature.TREE.configured(convert(BerryType.LIECHI, new LiechiFoliagePlacer(), new StraightTrunkPlacer(1, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> LEPPA_TREE = register(
-        "leppa_berry_tree",
-        Feature.TREE.configured(convert(BerryType.LEPPA, new LeppaFoliagePlacer(), new StraightTrunkPlacer(2, 0, 0)).ignoreVines().build())
+      "leppa_berry_tree", Feature.TREE.configured(convert(BerryType.LEPPA, new LeppaFoliagePlacer(), new StraightTrunkPlacer(2, 0, 0)).ignoreVines().build())
     );
     public static final ConfiguredFeature<TreeConfiguration, ?> GINEMA_TREE = register(
         "ginema_berry_tree",
@@ -567,16 +541,12 @@
     public static final ConfiguredFeature<SurfaceBiomeFeatureConfig, ?> DAWN_ORE = register(
         "ore_dawn_stone",
         ((Feature)PixelmonFeature.ORE_DAWN.get())
-            .configured(
-                new SurfaceBiomeFeatureConfig(Blocks.STONE.defaultBlockState(), PixelmonBlocks.dawn_stone_ore.defaultBlockState(), BiomeCategory.PLAINS)
-            )
+         .configured(new SurfaceBiomeFeatureConfig(Blocks.STONE.defaultBlockState(), PixelmonBlocks.dawn_stone_ore.defaultBlockState(), BiomeCategory.PLAINS))
     );
     public static final ConfiguredFeature<SurfaceBiomeFeatureConfig, ?> DUSK_ORE = register(
         "ore_dusk_stone",
         ((Feature)PixelmonFeature.ORE_DUSK.get())
-            .configured(
-                new SurfaceBiomeFeatureConfig(Blocks.STONE.defaultBlockState(), PixelmonBlocks.dusk_stone_ore.defaultBlockState(), BiomeCategory.SWAMP)
-            )
+         .configured(new SurfaceBiomeFeatureConfig(Blocks.STONE.defaultBlockState(), PixelmonBlocks.dusk_stone_ore.defaultBlockState(), BiomeCategory.SWAMP))
     );
     public static final ConfiguredFeature<SurfaceCategoryFeatureConfig, ?> SHINY_ORE = register(
         "ore_shiny_stone",
@@ -679,8 +649,7 @@
         Feature.RANDOM_PATCH
             .configured(
                 new GrassConfigurationBuilder(
-                        new SimpleStateProvider(((Block)BlockRegistration.ULTRA_DESERT_CACTUS.orElse(Blocks.CACTUS)).defaultBlockState()),
-                        new ColumnPlacer(1, 3)
+                  new SimpleStateProvider(((Block)BlockRegistration.ULTRA_DESERT_CACTUS.orElse(Blocks.CACTUS)).defaultBlockState()), new ColumnPlacer(1, 3)
                     )
                     .tries(15)
                     .noProjection()
@@ -777,8 +746,7 @@
         Feature.RANDOM_SELECTOR
             .configured(
                 new RandomFeatureConfiguration(
-                    ImmutableList.of(ULTRA_JUNGLE_TREE.weighted(0.2F), ULTRA_JUNGLE_TALL_TREE.weighted(0.7F), ULTRA_JUNGLE_BUSH.weighted(0.5F)),
-                    ULTRA_JUNGLE_TREE
+               ImmutableList.of(ULTRA_JUNGLE_TREE.weighted(0.2F), ULTRA_JUNGLE_TALL_TREE.weighted(0.7F), ULTRA_JUNGLE_BUSH.weighted(0.5F)), ULTRA_JUNGLE_TREE
                 )
             )
             .decorated(Decorators.HEIGHTMAP_SQUARE)
