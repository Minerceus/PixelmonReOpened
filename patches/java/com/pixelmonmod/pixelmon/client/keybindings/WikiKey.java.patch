--- a/com/pixelmonmod/pixelmon/client/keybindings/WikiKey.java
+++ b/com/pixelmonmod/pixelmon/client/keybindings/WikiKey.java
@@ -56,7 +56,7 @@
                 }
 
                 if (id != null && name != null) {
-                    name = name.trim().replace(":", "%3A").replace(" ", "_").replace("\u00e9", "e");
+               name = name.trim().replace(":", "%3A").replace(" ", "_").replace("é", "e");
                     String resourceDomain = id.getNamespace();
                     String baseURL = null;
                     if (resourceDomain.equalsIgnoreCase("pixelmon")) {
